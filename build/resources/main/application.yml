spring:
  mvc:
    format:
      date: iso
  datasource:
    url: jdbc:postgresql://localhost:5433/postgres
    username: postgres
    password: pass
    driver-class-name: org.postgresql.Driver
  jpa:
    properties.hibernate:
      default_schema: coursework_marketplace
      show_sql: true
      format_sql: true
      hbm2ddl.auto: validate
    open-in-view: false

app:
  image:
    bucket: /home/artem/IdeaProjects/projects/coursework-marketplace/images